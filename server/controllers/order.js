// const { addOrder } = require('../database/queries/order');

// exports.addOrder = (req, res, next) => {
//   console.log(req.body);
//   const { createdAt, totalPrice, tableNumber } = req.body;

//   addOrder(createdAt, totalPrice, tableNumber)
//     .then(console.log);
// };
